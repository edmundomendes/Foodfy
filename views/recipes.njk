{% extends 'layout.njk' %}
   {% block title %}
        <title>Recipes</title>
    {% endblock title %}
{%block header%}
    <div class="links">    
    <a href="/about">Sobre</a>
    <a href="/recipes" style='color: blue; font-weight:bold;'>Receitas</a>
</div>
{% endblock%}
{% block content %}
    <section class="recipes">
        {% for item in items %}
            <div class="card-recipes" id='recipes_link'>
                <div class="recipes-images">
                    <img src={{ item.image }}></div>
                    <div class="recipes-info">
                        <h1>{{ item.title }}</h1>
                        <p>{{ item.author }}</p>
                    </div>
                </div>
            {% endfor %}
             {% for item in items %}
            <div class="card-recipes">
                <div class="recipes-images">
                    <img src={{ item.image }}></div>
                    <div class="recipes-info">
                        <h1>{{ item.title }}</h1>
                        <p>{{ item.author }}</p>
                    </div>
                </div>
            {% endfor %}
        </section>
     </div>
     <script src="/script.js"></script>
{% endblock %}